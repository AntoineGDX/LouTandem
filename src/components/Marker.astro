---
// Information manquante, affichée telle quelle pour rester visible.
interface Props { text?: string }
const { text = '[À CONFIRMER PAULO]' } = Astro.props;
---
<span class="a-confirmer">{text}</span>
